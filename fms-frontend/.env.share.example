# Copy to .env.local when sharing localhost with a client (e.g. via ngrok).
# Replace with YOUR backend tunnel URL (from ngrok/localtunnel for port 8000).
VITE_API_BASE_URL=https://YOUR-BACKEND-TUNNEL-URL.ngrok-free.app
